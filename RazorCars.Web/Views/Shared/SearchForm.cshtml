@model IEnumerable<RazorCars.Web.Models.CarType>

<div >
    @using (Html.BeginForm("Login", "Account", FormMethod.Post, htmlAttributes: new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <label>Select Make</label>
        <select class="browser-default">
            <option value="" disabled selected>Make</option>
            @*@foreach (var item in Model)
            {
                for (int i = 1; i < item.Make.Count(); i++)
                {

                    <option value="@i">@item.Make</option>
                }

            }*@
        </select>

        <label>Select Model</label>
        <select class="browser-default">
            <option value="" disabled selected>Model</option>
            @*@foreach (var item in Model)
            {
                for (int i = 1; i < item.Model.Count(); i++)
                {

                <option value="@i">@item.Model</option>
                }

            }*@
        </select>

        <label>Select Year</label>
        <select class="browser-default">
            <option value="" disabled selected>Year</option>
            @*@foreach (var item in Model)
            {
                for (int i = 1; i < item.Year.ToString().Count(); i++)
                {

                    <option value="@i">@item.Year</option>
                }

            }*@
        </select>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Search" class="btn btn-default" />
            </div>
        </div>

        @*<label>Choose Color</label>
            <select class="browser-default">
                <option value="" disabled selected>Choose your option</option>
                <option value="1">Option 1</option>
                <option value="2">Option 2</option>
                <option value="3">Option 3</option>
            </select>*@
    }

    @* Manufacturer *@
    @* Size *@

    @* Class *@

    @* Price Range *@

    @* Color *@


</div>